{
  "name": "raket-clone",
  "version": "1.0.0",
  "description": "Raket.ph clone - Crowdfunding Platform",
  "private": true,
  "scripts": {
    "install": "npm install && cd client && npm install && cd ../server && npm install",
    "build": "npm run install && cd client && npm run build",
    "start": "cd server && npm start",
    "netlify-build": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yourusername/raket-clone.git"
  },
  "keywords": [
    "crowdfunding",
    "react",
    "node",
    "mongodb",
    "express"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  },
  "dependencies": {
    "concurrently": "^8.0.1",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "mongoose": "^7.0.3",
    "multer": "^1.4.5-lts.1",
    "jsonwebtoken": "^9.0.0",
    "bcryptjs": "^2.4.3",
    "validator": "^13.9.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
